[pytest]
# Repo-root runs (CI: `uv run --project runtime pytest`) collect exactly the
# runtime suite and the onboarding suite; per-directory configs still apply
# when pytest is invoked inside runtime/.
testpaths = runtime/tests tests
pythonpath = runtime/src
